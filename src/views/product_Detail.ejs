<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Enlace a la hoja de estilo de Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
    
    <!-- Estilos CSS en línea -->
    <style>
      /* Establecer valores predeterminados para todos los elementos */
      * {
        margin: 0;
        box-sizing: border-box;
      }

      /* Definir variables CSS */
      :root {
        --default-padding: 0px;
        --default-margin: 0px;
      }

      /* Estilos para el cuerpo del documento */
      body {
        font-family: Arial, sans-serif;
        flex-direction: column;
        margin: 0;
        padding: 0;
        background-color: #f5f5f5;
      }

      /* Estilos para encabezado y pie de página */
      header, footer {
        background-color: #d6d1d0;
        color: rgb(7, 1, 1);
        padding: 1em;
        text-align: center;
      }

      /* Estilos para la sección de detalles del producto */
      .product-detail {
        display: flex;
        justify-content: space-around;
        margin-top: 2em;
        background-color: white;
        padding: 2em;
      }

      /* Estilos para la imagen del producto */
      .product-image {
        width: 300px;
        height: 300px;
      }

      /* Estilos para la información del producto */
      .product-info {
        display: flex;
        flex-direction: column;
        justify-content: space-between;
      }

      /* Estilos para el nombre del producto */
      .product-name {
        font-size: 1.5em;
        font-weight: bold;
      }

      /* Estilos para el precio del producto */
      .product-price {
        color: green;
        font-size: 1.2em;
      }

      /* Estilos para el botón de agregar al carrito */
      button {
        background-color: #0a0a0a;
        color: white;
        border: none;
        padding: 10px 20px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        margin: 4px 2px;
        cursor: pointer;
      }

      /* Estilos para el botón de agregar al carrito al pasar el ratón sobre él */
      button:hover {
        background-color: #0072BB; /* Azul Francia */
      }

      /* Estilos para la sección de productos relacionados */
      .related-products {
        display: flex;
        flex-direction: row;
        text-align: center;
        font-weight: bold;
        overflow-x: auto;
        flex-wrap: wrap;
      }

      /* Estilos para las imágenes de los productos relacionados */
      img {
        width: 300px;
        height: 300px;
      }

      /* Estilos para cada producto relacionado */
      .related-product {
        flex: 0 0 auto;
        flex-wrap: wrap;
        font-weight: normal;
        width: 200px;
        margin-right: 1em;
        width: 400px;
        height: 400px;
      }

      /* Estilos para la sección de comentarios */
      .comments {
        margin-top: 2em;
        padding: 1em;
        font-weight: bold;
      }

      /* Estilos para cada comentario */
      .comment {
        border-bottom: 1px solid #ccc;
        padding: 1em 0;
        font-weight: normal;
      }

      /* Estilos para el autor del comentario */
      .comment-author {
        font-weight: bold;
      }

      /* Estilos para el menú de navegación */
      .nav-menu {
        list-style-type: none;
        margin: 0;
        padding: 0;
        overflow: hidden;
      }

      /* Estilos para cada elemento del menú de navegación */
      .nav-menu li {
        float: left;
      }

      /* Estilos para cada enlace del menú de navegación */
      .nav-menu li a {
        display: block;
        color: rgb(8, 7, 7);
        text-align: center;
        padding: 14px 16px;
        text-decoration: none;
      }

      /* Estilos al pasar el ratón sobre cada enlace del menú de navegación */
      .nav-menu li a:hover {
        background-color: #0072BB;
      }

      /* Estilos para el icono de perfil en el menú de navegación */
      .profile-icon {
        width: 20px;
        height: 20px;
        margin-right: 5px;
      }
    </style>
  
